@model hastanerandevu.Models.Entities.personel
@{
    Layout = null;
}

<!DOCTYPE html>
<html>
<head>
    <Title>
        Admin Giriş
    </Title>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-Zenh87qX5JnK2Jl0vWa8Ck2rdkQ2Bzep5IDxbcnCeuOxjzrPF/et3URy9Bv1WTRi" crossorigin="anonymous">
    <link href='https://fonts.googleapis.com/css?family=Poppins' rel='stylesheet'>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            height: 90vh;
            display: flex;
            align-items: center;
            justify-content: center;
            font-family: 'Poppins';
        }

        h1 {
            font-size: 36px;
            margin-bottom: 25px;
        }

        .giris {
            height: min-content;
            padding: 20px;
            border-radius: 12px;
            background: #ffffff;
        }

        input[type="button"] {
            font-size: 20px;
            margin-top: 15px;
        }

        input[type="submit"] {
            font-size: 20px;
            margin-top: 15px;
        }

        .abc {
            margin-bottom: 12px;
        }

        #image {
            display: block;
            margin-left: auto;
            margin-right: auto;
            width: 100px;
        }

        #icon {
            width: 20px;
        }

        input::-webkit-outer-spin-button,
        input::-webkit-inner-spin-button {
            -webkit-appearance: none;
            margin: 0;
        }
    </style>
</head>

<body>
    @using (Html.BeginForm())
    {
        @Html.AntiForgeryToken()
        @Html.ValidationSummary(true)

        <div class="giris">

            <img src="~/img/calendar.png" id="image" alt="Takvim logosu">
            <h1 class="text-danger ">Randevu Alma Sistemi</h1>

            <form class="needs-validation">

                <div class="abc was-validated">
                    <img src="/img/userlogin.png" alt="kullanıcı logo" id="icon">
                    <label class="form-label" for="PUsername">Kullanıcı Adı</label>
                    <br>
                    @Html.EditorFor(model => model.PUsername, new
                    {
                        htmlAttributes = new { @class = "form-control" }
    })
                    <div class="text-danger">
                        @Html.ValidationMessageFor(model => model.PUsername, "", new { @class = "text-danger" })
                    </div>
                </div>

                <div class="abc was-validated">
                    <img src="~/img/loginkey.png" alt="anahtar" id="icon">
                    <label class="form-label" for="PPasword">Parola</label>
                    <br>
                    @Html.EditorFor(model => model.PPasword, new { htmlAttributes = new { @class = "form-control" } })
                    <div class="text-danger">
                        @Html.ValidationMessageFor(model => model.PPasword, "", new { @class = "text-danger" })
                    </div>
                </div>

                <input class="btn btn-success w-100 text-bg-danger" type="submit" value="Giriş Yap">
                <br>

                @if (ViewBag.Notification != null)
                {
                    <div class="bg-light text-danger">
                        @ViewBag.Notification
                    </div>
                }
            </form>
        </div>
    }

</body>
</html>